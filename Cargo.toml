[package]
name = "claude-hindsight"
version = "1.0.1"
edition = "2021"
rust-version = "1.75"
authors = ["Codestz <est.estrada@outlook.com>"]
description = "20/20 hindsight for your Claude Code sessions"
homepage = "https://github.com/Codestz/claude-hindsight"
repository = "https://github.com/Codestz/claude-hindsight"
license = "MIT"
keywords = ["claude", "observability", "cli", "terminal-ui"]
categories = ["command-line-utilities", "development-tools"]
include = [
    "src/**/*",
    "web/out/**/*",
    "web/package.json",
    "build.rs",
    "Cargo.toml",
]

[dependencies]
# CLI framework
clap = { version = "4.5", features = ["derive", "cargo", "env"] }

# Web server (for `claude-hindsight serve`)
axum = { version = "0.8", features = ["macros"] }
tokio = { version = "1", features = ["full"] }
tower = "0.5"
tower-http = { version = "0.6", features = ["cors", "trace"] }
rust-embed = { version = "8", features = ["mime-guess"] }
mime_guess = "2"
tokio-stream = "0.1"
futures-util = "0.3"

# JSON parsing
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

# Database
rusqlite = { version = "0.38", features = ["bundled"] }

# File watching
notify = { version = "8.2", default-features = false, features = ["macos_kqueue"] }

# Terminal UI
ratatui = "0.28"
crossterm = { version = "0.28", features = ["event-stream"] }
tui-tree-widget = "0.22"

# Error handling
thiserror = "1.0"
anyhow = "1.0"

# Utilities
chrono = { version = "0.4", features = ["serde"] }
dirs = "6.0"
toml = "1.0"

# Performance
once_cell = "1.19"
arboard = "3.4"
similar = "2.6"
seahash = "4.1"

# SCC (Semantic Context Capsule)
prost = "0.13"
zstd = "0.13"

[build-dependencies]
prost-build = "0.13"

[dev-dependencies]
tempfile = "3.12"
assert_cmd = "2.0"
predicates = "3.1"

[profile.release]
opt-level = 3
lto = "thin"
strip = true
codegen-units = 1

[profile.dev]
opt-level = 0

[lib]
name = "hindsight"
path = "src/lib.rs"


[[bin]]
name = "claude-hindsight"
path = "src/main.rs"
